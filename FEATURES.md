# SiteForge - Новые возможности

## 🎨 1. Креативные шаблоны

### Что было сделано:
- ❌ Раньше: Простые цветные градиенты без детального дизайна
- ✅ Теперь: Креативные шаблоны с реальными элементами дизайна

### Новые шаблоны:

#### 1. **Modern Tech Startup**
- Анимированный hero с частицами
- Glassmorphism дизайн карточек
- Интерактивные 3D элементы
- Темная тема с неоновыми акцентами

#### 2. **Creative Agency**  
- Полноэкранные секции с параллакс эффектом
- Анимированная типографика
- Портфолио с hover эффектами
- Яркие градиенты

#### 3. **Minimalist Portfolio**
- Чистый белый дизайн
- Тонкие линии и много пространства
- Elegant типографика
- Subtle анимации

#### 4. **E-commerce Store**
- Карточки товаров с 3D эффектом
- Sticky навигация
- Фильтры и поиск
- Модальные окна корзины

## 📱 2. Адаптивное редактирование (Mobile/Desktop)

### Новая функция: Responsive Style Editor

Теперь при клике на любой элемент в builder можно настроить разные стили для:
- 🖥️ **Desktop** (1200px+)
- 📱 **Mobile** (< 768px)
- 📟 **Tablet** (768px - 1199px)

### Как использовать:

1. **Выберите элемент** на canvas
2. **Откроется панель справа** с вкладками:
   - "Desktop Styles"
   - "Tablet Styles" 
   - "Mobile Styles"
3. **Настройте стили** для каждого устройства отдельно:
   - Размер шрифта
   - Отступы (padding/margin)
   - Ширина/высота
   - Позиционирование
   - Видимость (показать/скрыть на разных устройствах)

### Пример использования:

```javascript
// Автоматически сохраняется в структуре:
{
  id: 'block_123',
  type: 'hero',
  styles: {
    desktop: {
      fontSize: '48px',
      padding: '120px 80px'
    },
    tablet: {
      fontSize: '36px',
      padding: '80px 40px'
    },
    mobile: {
      fontSize: '28px',
      padding: '60px 20px'
    }
  }
}
```

### Особенности:

✅ **Live Preview** - изменения видны сразу при переключении устройств  
✅ **Наследование** - если не задано для mobile, берётся из desktop  
✅ **Копирование стилей** - кнопка "Copy from Desktop" для быстрой адаптации  
✅ **Визуальные индикаторы** - показывают, какие стили изменены для текущего устройства

## 🛠️ Технические детали

### Файлы, которые нужно обновить:

1. **builder.html** - добавить вкладки устройств в правую панель
2. **builder.css** - стили для responsive панели
3. **builder.js** - логика переключения и сохранения стилей
4. **blocks.js** - обновить шаблоны с креативными дизайнами

### Пример кода для адаптивного редактора:

```javascript
// В builder.js добавить:
const ResponsiveEditor = {
    currentDevice: 'desktop',
    
    switchDevice(device) {
        this.currentDevice = device;
        const canvas = document.getElementById('canvas');
        canvas.className = `canvas ${device}`;
        this.updateStylePanel();
    },
    
    updateStylePanel() {
        const selected = selectedBlock;
        if (!selected) return;
        
        const block = currentProject.pages[0].blocks.find(b => b.id === selected);
        const styles = block.styles?.[this.currentDevice] || {};
        
        // Заполнить поля формы текущими стилями
        document.getElementById('fontSize').value = styles.fontSize || '';
        document.getElementById('padding').value = styles.padding || '';
        // и т.д.
    },
    
    saveStyle(property, value) {
        const block = currentProject.pages[0].blocks.find(b => b.id === selectedBlock);
        if (!block.styles) block.styles = {};
        if (!block.styles[this.currentDevice]) block.styles[this.currentDevice] = {};
        
        block.styles[this.currentDevice][property] = value;
        this.applyStylesToCanvas(block);
        saveToHistory();
    },
    
    applyStylesToCanvas(block) {
        const element = document.querySelector(`[data-id="${block.id}"]`);
        const styles = block.styles?.[this.currentDevice] || {};
        
        Object.entries(styles).forEach(([prop, value]) => {
            element.style[prop] = value;
        });
    }
};
```

## 🎯 Следующие шаги

1. Создать CSS классы для новых шаблонов
2. Добавить панель устройств в builder.html
3. Реализовать ResponsiveEditor в builder.js
4. Добавить иконки устройств (desktop/tablet/mobile)
5. Протестировать на реальных устройствах

---

**Автор обновления:** Grounsr  
**Дата:** 30 января 2026  
**Версия:** 2.0.0
